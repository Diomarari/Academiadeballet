name: Alerta Push a Main

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Enviar notificación a ntfy correcto
        run: |
          curl -d " Nuevo push en main - Repo actualizado correctamente otra vez" https://ntfy.sh/billiem4r